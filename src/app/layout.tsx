import { Provider } from "react-redux";
import { store } from "../../redux/store";
import { Amplify } from "aws-amplify";
import { Providers } from "../../redux/provider";
import AuthClientComponent from "../components/Auth.client";
import AuthServerComponent from "@/components/Auth.server";
import config from "../aws-exports";
Amplify.configure({ config, ssr: true });

export const metadata = {
	title: "Next.js",
	description: "Generated by Next.js",
};

const user = await AuthServerComponent();
// console.log("user in layout from aSC:", user);
console.log("blah blah blah");

export default async function RootLayout({
	children,
}: {
	children: React.ReactNode;
}) {
	return (
		<html lang="en">
			<body>
				<Providers>
					<AuthClientComponent user={user} />
					{children}
				</Providers>
			</body>
		</html>
	);
}
